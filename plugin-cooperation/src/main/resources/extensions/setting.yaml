apiVersion: v1alpha1
kind: Setting
metadata:
  name: cooperation-setting
spec:
  forms:
    - group: security
      label: 安全与限流
      formSchema:
        - $formkit: number
          name: rateLimitSeconds
          label: 限流秒数（IP 维度）
          value: 60
          help: "同一 IP 多少秒内仅允许提交一次，默认 60"
