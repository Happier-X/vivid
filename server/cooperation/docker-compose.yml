services:
  cooperation:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./cooperations.db:/app/cooperations.db
      - ./cooperations.log:/app/cooperations.log
    restart: unless-stopped
    environment:
      - PORT=8000
